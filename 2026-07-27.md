# 2026-07-27 — `linear` row completed, `star` opened, three export guards added

> **Where the matrix stands: 7 / 24.** Both linear rows are closed at 3/3, and
> `star · wormhole · r1` is captured and recorded. Every recorded cell is
> **0 FAIL**, and the two attack signatures reproduce on independent boards.

```
topology  attack     r1  r2  r3
-------------------------------
linear    blackhole  [x]  [x]  [x]     ← complete
linear    wormhole   [x]  [x]  [x]     ← complete
star      wormhole   [x]  [ ]  [ ]
star      blackhole  [ ]  [ ]  [ ]
tree      blackhole  [ ]  [ ]  [ ]
tree      wormhole   [ ]  [ ]  [ ]
partial   blackhole  [ ]  [ ]  [ ]
partial   wormhole   [ ]  [ ]  [ ]
```

---

## 🎯 Milestone alignment

**M4 (*Phase-Controlled Experiment Execution*, 15%) — on track, 7/24.** Its gate is
stated in `run_matrix.py`: a cell is marked done "only once its exported CSVs are present
**AND pass `validate_integrity.py`**". Every recorded cell clears that:

| cell | files | validation |
|---|---|---|
| `linear · blackhole` r1–r3 | 21 | **21 PASS / 0 WARN / 0 FAIL** |
| `linear · wormhole` r1–r3 | 21 | **21 PASS / 0 WARN / 0 FAIL** |
| `star · wormhole` r1 | 7 | **7 PASS / 0 WARN / 0 FAIL** |

**M3 is a separate criterion and is NOT M4's gate.** `verify_topology.py` prints its
verdict on every run it is pointed at, which makes an attack run look like it failed a
milestone it is not being graded on. Per `thesis-deviate.md` D-4, M3 is evidenced against
the **baseline** run, and the runbook states baseline is "**not** part of the M4 24".

**M7 — per-run NaN counts are expected, not gaps.** `thesis-deviate.md`: *"M7's 'no feature
is uniformly NaN' applies to the assembled dataset, not to any one run… 16/16 across the
combined matrix."* Current per-run population is consistent with that (below).

---

## 📊 EDA / feature-table state

| dataset | windows | nodes | labels (benign / attack) | EDA outputs |
|---|---|---|---|---|
| `blackhole · linear` (r1–r3 pooled) | 2470 | 6 | 1820 / 650 | 28 |
| `wormhole · linear` (r1–r3 pooled) | 2470 | 6 | 1819 / 651 | 29 |
| `wormhole · star` (r1) | 884 | 6 | 667 / 217 | 23 |

Feature population, confirming each feature lands where the thesis says it should:

| feature | blackhole·linear | wormhole·linear | wormhole·star |
|---|---|---|---|
| `RetryRate` (all nodes) | 2470 | 2470 | 884 |
| `PDR` (probing victims only) | 1651 | 1647 | 599 |
| `ForwardingRatio` (blackhole relay only) | **413** | 0 | 0 |
| `TunnelIntensity` (tunnel ends only) | 0 | **854** | **306** |
| `TunnelLatency` (Node B only, tunnel live) | 0 | **114** | **37** |

Every zero above is correct by design — a wormhole run has no blackhole relay, and a
blackhole run has no tunnel. Discard fractions stayed at **0.2 – 0.8 %** across all three.

### 🔭 New: tunnel-end projection

The whole-mesh PCA/t-SNE **cannot** show the tunnel features: they exist on 2 of 6 nodes
(~67 % NaN) and `eda.py` drops anything past 50 % NaN. The result was a projection built
from the features a wormhole barely touches — an honest answer to *"is the attack visible
in what every node measures?"*, but silent on the tunnel, reading as though there were no
evidence.

`plot_tunnel_end_projection()` adds a companion plot over the nodes that actually carry a
tunnel, with the tunnel columns kept in — **854 windows on linear, 306 on star**. Both plots
are written; neither replaces the other. It skips itself on baseline/blackhole tables.

---

## 🔬 Attack signatures — measured on both sides

**Blackhole** — the attacker's counters and the root's arrivals log are separate boards
and separate files, and they agree exactly:

| phase | probes received | tx (forwarded) | retry (dropped) | root arrivals |
|---|---|---|---|---|
| 0 baseline | 0 → 1439 | 0 → 1439 | 0 | 1436 rows |
| 1 blackhole | 1439 → **2159** | **1439 → 1439** | 0 → **720** | **0 rows** |
| 3 cooldown | 2159 → 2643 | 1439 → 1923 | 720 | 483 rows |

720 received during the window, 720 dropped, zero forwarded, zero arriving.

**Wormhole** — duplicate `(src_mac, seq_num)` deliveries, stable across topologies:

| run | duplicates | multiplicity | source |
|---|---|---|---|
| linear r1 | 181 | ×2.00 | Node B `F4:2D:C9:73:E6:18` |
| linear r3 | 181 | ×2.00 | Node B |
| **star r1** | **180** | ×1.25 overall | Node B |

Zero duplicates in baseline or cooldown in every run. On star the count was **predicted
before the root was exported** — node5 showed `probes 0→180` and node6 showed
`retry 0→180` during phase 2, and the root then logged 180 duplicates.

---

## 🛠️ Three guards added after three separate failures

Each failure surfaced ~20 minutes downstream, when `features.py` finally refused the data.
All three now fail at the moment they happen.

### 1. `export_logs.py` — device sent the wrong file

`--role root` issues `EXPORT_LOGS` then `EXPORT_ARRIVALS`. On `star · wormhole · r1` the
second streamed **`telem.csv`** instead, so a good run was saved under an `_arrivals.csv`
name holding a copy of the telemetry. Both root files had the identical first data row.

`_check_expected_schema()` compares the stream's header against the file requested
(arrivals carry `src_mac`/`seq_num`, telemetry does not). On mismatch it **quarantines** the
capture as `....csv.rejected` — outside every downstream glob — and **skips `--delete`**, so
the board keeps the original.

> ⚠️ **Root cause not established.** Most likely a lost `END_OF_FILE` making the second
> capture re-read the first file, but that is unproven. The guard makes it detectable and
> non-destructive; it does not prevent the device from doing it again.

### 2. `trim_run.py` — stale files in `trimmed/`

That folder is written and never cleaned. The bad arrivals file was deleted from the raw
folder and re-pulled correctly, but its trimmed copy stayed — and every analysis tool reads
the **folder**, so `features.py` loaded both and died on the stale one, *after* the
re-export had already fixed the problem.

`_warn_on_stale_outputs()` lists anything in the output folder with no raw source. It warns
rather than deletes: derived data, but one command from being an only copy.

### 3. `trim_run.py` — duplicate captures

Exports are timestamped, so re-exporting or re-running a cell **adds** a file rather than
overwriting. Both copies then load and that board is counted twice. This one produced **no
error at all** — the root reached 264 windows against ~155 for every other node, and the
report looked plausible.

`_warn_on_duplicate_captures()` parses filenames into
`(role, label, topology, attack, repeat, kind)` and flags any key with more than one file.

**Audit:** all four existing capture folders — baseline·linear, blackhole·linear,
wormhole·linear, wormhole·star — are clean under both new checks.

### Also fixed

- `features.py` NaN hints were static text keyed on the column name, so a wormhole run
  printed `TunnelIntensity … [ATTACKER-ONLY: needs a wormhole run]` while that column held
  854 real values. Now reports what the data shows.
- `run_matrix.py` decoded the validator's output with the console codepage, printing every
  em-dash as mojibake. Needed **both** halves: `PYTHONIOENCODING=utf-8` on the child *and*
  `encoding="utf-8"` on the parent.
- `board_check.py` named boards by COM port (`-> COM26`) for a board sitting on COM20. Now
  reports **node numbers**, matching what `--label` uses.
- The runbooks' `MUST be 7` trimmed-file check now reads **7 per repeat** — a correct
  3-repeat run shows 21 and looked like a failure.

---

## ⚠️ Open items

**1. `star` fails the M3 stability criterion — cause not established.**
r1 was run twice. First placement: converge 99–153 s. Boards moved much closer to the root
(join times from the root's own log: **1.3 – 6.7 s**), and it still reports
`Converged within 60s : NO`.

The second run's data shows a **different** mechanism from the first — nodes *dropping and
re-attaching*, not failing to find the root:

```
node3   L2 → L-1 → L2 → L-1 → L2 → L-1 → L2   settling at 105s
node5/6 L2 → L-1 → L2 → L-1 → L2              settling at  99s
```

**Every one of these is in phase 0. Zero during the attack window** — which is why the
signature is clean. Hypothesis, unproven: structural to star, where all five children
associate with one root AP (`ap_assoc_expire(10)`), whereas linear gives each node at most
one child. The cheap test is a `baseline · star` run.

**2. Baseline controls captured for linear only.** `star`, `tree` and `partial` baseline
folders are empty. Decision taken: **use each attack run's phase 0 as the control instead**
— it is a better-matched reference (identical firmware, roles and session; the attack is
the only variable), where a separate baseline run differs in firmware *and* node count
(5 probing victims vs 4). **To be recorded in `thesis-deviate.md`.**

**3. Repeats share a topology class, not a placement.** Across linear wormhole r1–r3 the
A↔B gap went adjacent → 2 hops → 3 hops; parents are chosen by signal at boot. The
arrivals signature held anyway because the tunnel is a wire, but radio-path features
(`RSSI_Hop_Diff`, `LatencyHopRatio`, `HopStabilityDuration`) legitimately vary between
repeats.

**4. Analysis pools all repeats.** `preprocess.py` globs every telem file in a folder and
`feature_table.csv` carries no repeat column, so per-repeat variance cannot be separated.
Fine for M8 separability; a gap for per-repeat statistics.

**5. `node2` / `node4` labels unconfirmed.** `ATTACKS-Commands.md` disagreed with
`board_check.py` on which MAC is which; the two-source majority was used and both entries
marked. No effect on data — every CSV keys on MAC — but the human label may be swapped.

---

## 🔜 Next

1. 🔴 **M4** — 17 runs remain. `star · blackhole` r1–r3 next (boards already placed for
   star; the UART wire only matters for wormhole).
2. 🟡 **Record the baseline-control decision** in `thesis-deviate.md` as a deviation, with
   the better-matched-control reasoning.
3. ⚪ **Optional** — `baseline · star` to settle whether the phase-0 churn is the topology
   or the wormhole firmware.
4. ⚪ **Optional** — a tunnel-end-only or zero-imputed projection is now available; consider
   which the write-up should lead with.

### ✅ Habits that held up tonight

1. **`--autorecord`, never `--record --repeat N`.** No flag to mistype.
2. **Check the arrivals header before analysing.** It caught the wrong-file export.
3. **Never `--delete` on a re-export.** The board's copy is the only recovery path — and
   `export_logs.py` now enforces this on a failed schema check.
4. **Read the root's monitor log during the run.** The `Child connected:` lines gave the
   join times that proved the radio was fine, which the trimmed CSVs alone would not have.
